#include<iostream.h>
#include<conio.h>
void main()
{
 clrscr();
 int arr[5];                        //declaring an array which can contain 5 elements
 cout<<"Enter elements: ";
 for(int i=0;i<5;i++)
  cin>>arr[i];                      //getting input from the user
 cout<<"You entered: ";
 for(i=0;i<5;i++)
  cout<<endl<<*(arr+i);            //using pointers to display array elements
 getch();
} 
 
